


export const defaultOptions = Object.freeze({
  forceCrop: true,
  imageLockUniScaling: true,
  cropLockUniScaling: false,

  devMode: true,

});


export const defaultCropzone = Object.freeze({
  x: 0,
  y: 0,
  vWidth: 400,
  vHeight: 400,
});

